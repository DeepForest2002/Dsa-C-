4,5,6,7,0,1,2,3/// mid= index 3=7 (if left half is sorted) arr[low]<=arr[mid]find the min element in left half and then right half (not optimal)


# Find how many times has a array been rotated

Find the minimum in the sorted rotated array and the index of that element is the ans but with some slight change

# Find the single element in a sorted Array

