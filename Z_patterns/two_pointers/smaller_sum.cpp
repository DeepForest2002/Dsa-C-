#include <bits/stdc++.h>
using namespace std;

int smaller_sum_count(vector<int>arr, int target){
    sort(arr.begin(), arr.end());
    for (int i = 0; i < arr.size() - 2;i++){
        int l = i + 1, r = arr.size() - 1;
        while(l<r){
            
        }
    }
}

int main(){
    vector<int> arr = {-2,0,1,3};
    return 0;
}